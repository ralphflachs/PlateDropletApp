<Window x:Class="PlateDropletApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:PlateDropletApp.ViewModels"
        xmlns:converters="clr-namespace:PlateDropletApp.Converters"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Plate Droplet Data Display" Height="600" Width="800">
    <Window.Resources>
        <converters:IsLowDropletToBrushConverter x:Key="IsLowDropletToBrushConverter"/>
        <converters:IsLowDropletToDisplayTextConverter x:Key="IsLowDropletToDisplayTextConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Controls -->
            <RowDefinition Height="Auto" />
            <!-- Summary -->
            <RowDefinition Height="*" />
            <!-- Plate Grid -->
        </Grid.RowDefinitions>

        <!-- Controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Select Plate Data" Command="{Binding BrowseCommand}" Width="150" Margin="0,0,10,0"/>
            <TextBlock Text="Droplet Threshold:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <xctk:IntegerUpDown Value="{Binding DropletThreshold, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, 
                NotifyOnValidationError=True}" Width="100" Minimum="0" Maximum="500"/>
            <Button Content="Update" Command="{Binding UpdateThresholdCommand}" Width="100" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Summary -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Summary :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="Total Well Count: " Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalWellCount}" VerticalAlignment="Center"/>
            <TextBlock Text="Total well count with low droplets: " Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding LowDropletWellCount}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Plate Grid -->
        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Plate.Wells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="{Binding Plate.Rows}" Columns="{Binding Plate.Columns}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="0.5" Background="{Binding IsLowDroplet, Converter={StaticResource IsLowDropletToBrushConverter}}">
                            <TextBlock Text="{Binding IsLowDroplet, Converter={StaticResource IsLowDropletToDisplayTextConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
